name: mvvm_architecture_notifier
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  hooks_riverpod: ^1.0.4
  freezed: ^2.0.3+1
  freezed_annotation: ^2.0.3
  go_router: ^3.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.11
  json_serializable: ^6.2.0

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
